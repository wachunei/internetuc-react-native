import React from 'react';
import {
  Scene,
} from 'react-native-router-flux';

import Login from '../components/Login';

export default (
  <Scene
    key="Login"
    component={Login}
    initial
  />
);
