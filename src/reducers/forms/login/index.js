const initialState = {
  username: '',
  password: '',
  error: '',
};

export default function login(state = initialState, action) {
  switch (action.type) {
    default: {
      return state;
    }
  }
}
