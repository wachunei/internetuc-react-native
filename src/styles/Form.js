import {
  StyleSheet,
} from 'react-native';

import colors from '../config/colors';

export default StyleSheet.create({
  formBox: {
    justifyContent: 'center',
    flex: 1,
  },
  buttonsWrapper: {
    marginTop: 20,
  },
  disabledStyle: {
    opacity: 0.3,
  },
});
